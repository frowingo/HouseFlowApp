
import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("This is the old ContentView - not used in the demo")
                .padding()
        }
    }
}

#Preview {
    ContentView()
}
